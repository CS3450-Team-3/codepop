// this is an example of a type definition. modify as needed.

export type Drink = {
    id: string;
    name: string;
    sizeOz: number;
    baseFlavor: string;
    toppings: string[];
    price: number;
};